numpy>=1.24
PyYAML>=6.0
pytest>=7.4
matplotlib>=3.7
SciencePlots>=2.1
h5py>=3.8
# Optional but recommended for unstructured nearest-neighbour interpolation
# (HEC-RAS / LISFLOOD cell centres). A NumPy brute-force path exists for tests.
scipy>=1.10

# Optional — GeoTIFF terrain via GDAL. Not required for HDF adapters.
# rasterio>=1.3

# Optional — paper Sparse GP (GPflow SGPR). CPython only; not PyPy.
# Do not install these into a PyPy interpreter.
#
# Python 3.12 (verified 2026-08-14 on Windows):
#   pip install "gpflow>=2.10" "tensorflow>=2.16"
#   -> gpflow 2.11.1 + tensorflow 2.21.0 (forces numpy<2)
#
# Python 3.10–3.11:
#   pip install "gpflow>=2.9,<2.10" "tensorflow>=2.13,<2.16"
#
# If that install fails, the NumPy RBF GP fallback is used automatically.
# Saved NPZ weights can be reloaded for prediction without retraining.
